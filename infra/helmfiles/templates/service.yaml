apiVersion: v1
kind: Service
metadata:
  name: raffle-ui-service
  labels:
    app: raffle-ui
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: raffle-ui